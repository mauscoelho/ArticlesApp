import { Filter } from '../contansts';

const initialState = Filter.Default;

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
