import React from 'react';
import { View, Text } from 'react-native';

const ArticleItem = () => (
  <View>
    <Text>
      {'simple test storybook'}
    </Text>
  </View>
);

export default ArticleItem;
